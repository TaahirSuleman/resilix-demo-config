# Resilix automated remediation
# Incident: INC-3428b449
# Action: fix_code
# Summary: A recent configuration update to 'infra/dns/coredns-config.yaml' introduced an excessive volume of DNS target updates, causing a propagation backlog that overwhelmed the resolver's health check subsystem.
