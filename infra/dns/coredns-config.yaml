# Resilix automated remediation
# Incident: INC-0e308fc4
# Action: fix_code
# Summary: A configuration update to 'infra/dns/coredns-config.yaml' introduced a bottleneck in the propagation logic, resulting in a massive backlog of health check updates and causing DNS targets to flap.
