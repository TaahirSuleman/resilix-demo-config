# Resilix automated remediation
# Incident: INC-c9cb0d51
# Action: fix_code
# Summary: A recent configuration update to 'infra/dns/coredns-config.yaml' introduced a propagation bottleneck, causing a massive backlog in the health check queue and resulting in flapping target states.
