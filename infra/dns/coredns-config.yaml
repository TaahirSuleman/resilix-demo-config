# Resilix automated remediation
# Incident: INC-12348d76
# Action: fix_code
# Summary: A recent configuration change to the CoreDNS setup in 'infra/dns/coredns-config.yaml' introduced a performance regression that caused a massive propagation backlog, leading to DNS target health flapping.
