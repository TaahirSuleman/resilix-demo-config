# Resilix automated remediation
# Incident: INC-e776d7a6
# Action: config_change
# Summary: A configuration update to 'infra/dns/coredns-config.yaml' introduced an inefficiency or bottleneck in the DNS resolver's target propagation mechanism, resulting in a large processing backlog and unstable health checks.
