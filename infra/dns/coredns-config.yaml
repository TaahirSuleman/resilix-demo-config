# Resilix automated remediation
# Incident: INC-556f5bd0
# Action: fix_code
# Summary: A recent update to 'infra/dns/coredns-config.yaml' introduced configuration settings that overwhelmed the health check propagation subsystem, creating a massive backlog that caused target health flapping and service timeouts.
