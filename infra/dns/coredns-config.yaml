# Resilix automated remediation
# Incident: INC-93b5a92c
# Action: config_change
# Summary: Aggressive or misconfigured health check parameters in 'infra/dns/coredns-config.yaml' led to a propagation backlog in 'dns-resolver', causing target health flapping and cascading timeouts.
